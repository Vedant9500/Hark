# Rust / Cargo
/target/
**/*.rs.bk
Cargo.toml.orig

# Local-only: seeded usage db for README screenshots
assets/screenshots/seed-usage.json
scripts/seed-screenshots.sh

# Debug / profiling
*.pdb
perf.data
perf.data.old
flamegraph.svg
*.profraw
*.profdata
callgrind.out.*
cachegrind.out.*

# Editor / IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~
.project
.classpath
.settings/
*.code-workspace

# OS junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Desktop.ini

# Logs & local runtime
*.log
*.tmp
*.temp
.cache/
docs/bench
docs/archive
feature.txt

# Env / secrets (if added later)
.env
.env.*
!.env.example

# Local personal scratch (not project docs)
todo.txt
*.local.md

# Optional local override configs
/config.local.toml
/hark.local.toml

# Test / coverage artifacts
/coverage/
/tarpaulin-report.html
lcov.info
*.lcov

# Packaging leftovers / local release output
*.deb
*.rpm
*.AppImage
# keep source/release archives under dist/ only (ignore accidental root tarballs)
/*.tar.gz
/*.zip
/dist/
/packaging/aur/pkg/
/packaging/aur/src/
/packaging/aur/*.tar.gz
/packaging/aur/*.pkg.tar*
